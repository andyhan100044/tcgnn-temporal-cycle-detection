# TCD Paper + TC-GNN Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (or subagent-driven-development) for task-by-task execution after approval.

**Goal:** Produce a complete bilingual (zh/en) TNNLS-submittable manuscript on Temporal Cycle Detection + TC-GNN, with runnable Python implementation and real Elliptic2 experimental results.

**Architecture:**
- **Paper layer**: Two independent LaTeX manuscripts sharing figures/tables — `paper/main_en.tex` (IEEEtran TNNLS format, ~13k words) and `paper/main_zh.tex` (中文完整对照). Bilingual independence means each can be reviewed/compiled without the other.
- **Code layer**: Python package `src/temporal_cycle/` with TemporalDFS, TemporalCycleSketch, TC-GNN, and an experiments runner. PyTorch + PyTorch Geometric for the GNN; NetworkX for utilities.
- **Data layer**: Real Elliptic2 dataset downloaded into `data/elliptic2/`. Fallback ladder: Elliptic2 → Elliptic1 → synthetic placeholder (last resort).

**Tech Stack:** Python 3.10+, PyTorch 2.x, PyTorch Geometric, NetworkX, pandas, scikit-learn, XGBoost, LaTeX (IEEEtran class), BibTeX.

---

## TNNLS Review-Driven Revisions (must address before drafting)

These items are added based on a senior-TNNLS review of `paperoutline.txt`. Each maps to a paper section.

| Review ID | Issue | Paper location | Action |
|---|---|---|---|
| W1 | Elliptic2 has only 49 illicit subgraphs — fatal for stats | §6 | Synthetic data extension (100/500/2000 injected cycles) + bootstrap 95% CI + DeLong test |
| W2 | NP-completeness proof missing | §2.4 + Appendix A | Full 3-SAT reduction with timestamped variable/clause gadgets |
| W3 | Theorem 2 approximation hardness unsupported | §2.4 + Appendix A.2 | Full reduction (k-Clique or OV), or soften to conjecture |
| W4 | Sketch FPR bound unjustified | §4.4 + Appendix B | Derive path-signature collision probability under Count-Min |
| W5 | "Trade ↔ crypto" isomorphism asserted, not proved | §2.5 | Reframe as "Structural Isomorphism Theorem" with explicit time/hop/amount mappings and error bounds |
| W6 | Baselines under-specified | §6.2 | For each baseline: cycle-level adaptation, hyperparameter protocol, candidate-set sharing rule |
| W7 | Constraint satisfaction rate is self-fulfilling | §6.3 | Remove as headline metric; keep only in ablations |
| m1 | Comparison table sparse | §5.4 | Expand to 8 rows (add params, latency, GPU memory) |
| m2 | Missing ablations | §6.4 | Add 3 ablations: static-GNN+sketch / mean-pool / TGN-memory-replace |
| m3 | §1.4 structure misaligned | §1.4 | Restructure roadmap to clarify §2.5 is methodological setup |
| m4 | No figure outline | All | Add 7 figures: architecture / case studies ×3 / training curves / sensitivity heatmap / isomorphic mapping |
| m5 | Unicode subscripts in math | All | Rewrite all equations with `\mathbf{v}_1` style |
| m6 | "本文提出" overuse | All | Reduce to ≤15 occurrences |
| m9 | Elliptic2 stats must match release | §6.1 | Verify numbers against official release notes |
| Q6 | Single-dataset validation risky | §6 | Add IBM AMLSim or Bitcoin Fraud Dataset as second dataset |

**New tasks added (see TaskList):** T10 (synthetic experiment extension), T11 (NP proof completion), T12 (isomorphism theorem), T13 (second dataset), T14 (figure generation), T15 (math typesetting pass).

---

## Phase 1 REVISION (per user 2026-06-13)

**Decision:** Use **Elliptic1** as primary dataset. Defer Elliptic2 to future work.

**Paper text (§6.1) — verbatim from user:**

> 本研究以 Elliptic1 数据集（203,769 节点，234,355 边）为主要实验平台，验证 TC-GNN 在时序闭环检测中的有效性。Elliptic1 的节点级标注通过第 X 节所述的候选环构建方法转化为环级监督信号。Elliptic2（49M 节点，196M 边）作为更大规模的验证场景，将在**未来工作**中通过子图采样策略进一步测试本框架的可扩展性。

**Reviewer Q&A (insert into §8.2 / rebuttal):**

| Reviewer concern | Response |
|---|---|
| "为什么不用 Elliptic2？它是更新的数据集" | "Elliptic2 的子图标注任务与本研究的环级检测目标不同，且其 88GB 规模超出本研究的计算资源。我们专注于方法验证而非规模竞赛。" |
| "Elliptic1 的节点级标注不够精准" | "我们通过时序 BFS + 价值守恒约束，将节点级信号转化为环级监督，这是本研究的方法论贡献之一。" |

**Data acquisition protocol:**
1. **Now**: Build synthetic generator matching Elliptic1 published statistics (203,769 nodes, 234,355 edges, 49 timesteps, illicit/licit node labels). Save to `data/elliptic1/synthetic_placeholder/`. Loader auto-detects synthetic vs real.
2. **Later**: When user provides Kaggle API token (or manually downloads `elliptic_txs_features.csv`, `elliptic_txs_edgelist.csv`, `elliptic_txs_classes.csv` to `data/elliptic1/raw/`), the loader transparently switches to real data. Same downstream code.

---

## Phase 0: Project Foundation (autonomous, ~30 min)

### Task 0.1: Initialize git + folder skeleton

**Files:**
- Create: `E:/00001tradesupervi/.gitignore`
- Create: `E:/00001tradesupervi/README.md`
- Create: `E:/00001tradesupervi/{src,tests,experiments,data,paper,figures,results}/.gitkeep`
- Create: `E:/00001tradesupervi/requirements.txt`

**Step 1:** `git init` in `E:/00001tradesupervi/`
**Step 2:** Write `.gitignore` excluding `data/elliptic2/raw/`, `__pycache__/`, `*.pyc`, `results/raw_runs/`, `.venv/`
**Step 3:** Create directory skeleton
**Step 4:** Write `requirements.txt` with pinned versions: `torch>=2.0`, `torch-geometric>=2.4`, `networkx>=3.0`, `pandas`, `scikit-learn`, `xgboost`, `numpy`, `matplotlib`, `seaborn`, `tqdm`
**Step 5:** `git add -A && git commit -m "chore: project skeleton"`

**Verification:** `git log --oneline` shows 1 commit; directory tree matches skeleton.

### Task 0.2: LaTeX toolchain check

**Step 1:** Run `pdflatex --version` (if missing, document fallback to Overleaf export)
**Step 2:** Run `bibtex --version`
**Step 3:** Create `paper/.latexmkrc` with `-pdf -interaction=nonstopmode`
**Step 4:** Document in README which envs are required (`texlive-full`, `texlive-lang-chinese` for xeCJK)

**Verification:** `cd paper && pdflatex -interaction=nonstopmode -draftmode main_en.tex` exits 0 on stub file (after Phase 6 writes it).

---

## Phase 1: Data Acquisition — Elliptic2 (~30 min – 2 hr, with fallback)

### Task 1.1: Locate Elliptic2 source

**Step 1:** WebSearch "Elliptic2 dataset download" and "Elliptic2 cryptocurrency subgraph classification"
**Step 2:** Try these URLs in order:
- `https://www.elliptic.co/elliptic-2` (or `/dataset`)
- `https://github.com/elliptic-co/elliptic2_dataset` (or similar)
- Kaggle: `https://www.kaggle.com/datasets/ellipticco/elliptic-2-dataset`
- HuggingFace: search "elliptic2"
**Step 3:** Inspect each landing page with WebFetch; identify which has `nodes.csv` (or similar) and `edges.csv` with illicit subgraph labels.

### Task 1.2: Download Elliptic2

**Step 1:** Write `data/download_elliptic2.py` that:
  - Takes URL(s) from a config
  - Streams download with `requests` + `tqdm`
  - Verifies checksum if published
  - Falls through fallback URLs until one succeeds
**Step 2:** Save raw files to `data/elliptic2/raw/` (gitignored)
**Step 3:** Write `data/preprocess_elliptic2.py` that:
  - Loads nodes (`txId`, `time_step`, `class` with 0=licit/1=illicit)
  - Loads edges (`txId1`, `txId2`, `time_step`)
  - Loads subgraph-level illicit labels (49 subgraphs)
  - Outputs `data/elliptic2/processed/{nodes.parquet, edges.parquet, illicit_subgraphs.json}`
**Step 4:** Sanity-check: print n_nodes, n_edges, n_illicit_subgraphs, time_step range.

**Verification:** `python data/preprocess_elliptic2.py` reports ≈204,769 nodes, ≈234,355 edges, 49 illicit subgraphs (matches paper claim).

### Task 1.3: Fallback ladder

If Elliptic2 is unreachable:
1. **Fallback A**: Elliptic1 (≈203k nodes, 234k edges, 4,545 illicit — node-level labels). Document the substitution in paper §6.1 and §8.2.
2. **Fallback B** (last resort): Synthetic generator `data/synth_generator.py` that mimics Elliptic2's reported statistics (degree distribution, time_step distribution, illicit subgraph sizes). Must be documented in §6.1 as "synthetic stand-in due to access restrictions."

**Verification:** Whatever data lands, `experiments/dataset_smoke.py` loads it and reports shape.

---

## Phase 2: Algorithm Implementation with TDD (~6–10 hr)

### Task 2.1: Temporal index structure (`src/temporal_cycle/indexing.py`)

**Files:**
- Create: `src/temporal_cycle/__init__.py`
- Create: `src/temporal_cycle/indexing.py`
- Create: `tests/test_indexing.py`

**TDD steps:**
1. Write test `test_out_neighbors_sorted_by_time`: building index for graph with 3 edges (a→b@5, a→c@3, a→d@7) returns out-neighbors in time order [c@3, b@5, d@7].
2. Run test → fail (no implementation).
3. Implement `TemporalIndex` with per-node sorted lists (sort by t) using bisect for `out_neighbors_in_window(v, t_lo, t_hi)`.
4. Run test → pass.
5. Tests for `in_neighbors_in_window`, `degree`, `n_edges`. Commit.

### Task 2.2: TemporalDFS — Algorithm 1 (`src/temporal_cycle/temporal_dfs.py`)

**Files:**
- Create: `src/temporal_cycle/temporal_dfs.py`
- Create: `tests/test_temporal_dfs.py`

**TDD steps:**
1. Test `test_finds_simple_3cycle`: toy graph with 3-cycle (A→B@t=1, B→C@t=2, C→A@t=3) — k=3, Δt=5, ε=0.1 → returns the cycle.
2. Test `test_rejects_violating_time_order`: same cycle but with t=3, t=2, t=1 (out of order) → returns empty.
3. Test `test_rejects_violating_window`: same cycle but Δt=1 → empty.
4. Test `test_rejects_value_imbalance`: weights 10, 10, 30 with ε=0.1 → empty.
5. Test `test_value_conservation_ok`: weights 10, 10, 20 with ε=0.1 → cycle returned.
6. Implement `find_temporal_cycles(G, k, dt, eps)` with recursive DFS, time/balance pruning.
7. Run all tests → pass. Commit.
8. Add `test_complex_graph_5node`: build graph with multiple cycles, verify enumeration completeness.

**Verification:** `pytest tests/test_temporal_dfs.py -v` all pass.

### Task 2.3: TemporalCycleSketch — Algorithm 2 (`src/temporal_cycle/temporal_sketch.py`)

**Files:**
- Create: `src/temporal_cycle/temporal_sketch.py`
- Create: `tests/test_temporal_sketch.py`

**TDD steps:**
1. Test `test_sketch_finds_cycle_5nodes`: 5-cycle across 3 time windows → found.
2. Test `test_sketch_recall_100`: comparing sketch output vs TemporalDFS exhaustive on a 100-edge graph — sketch must contain every DFS-found cycle (no false negatives).
3. Test `test_sketch_decoupled_space`: sketch memory ≈ O(w·d·m), independent of |E|.
4. Implement `TemporalCycleSketch` with Count-Min Sketch, time-windowed path prefixes.
5. Run tests → pass. Commit.

**Verification:** Recall test passes; space test passes on 10k-edge synthetic graph.

---

## Phase 3: TC-GNN Architecture (~4–6 hr)

### Task 3.1: Temporal Message Passing layer (`src/temporal_cycle/tc_gnn.py`)

**Files:**
- Create: `src/temporal_cycle/tc_gnn.py`
- Create: `src/temporal_cycle/losses.py`
- Create: `tests/test_tc_gnn.py`

**TDD steps:**
1. Test `test_temporal_mp_filters_old_neighbors`: passing in a graph with 2 outgoing edges (t=3 and t=1) where current path time is t=2 → only t=3 edge aggregates.
2. Test `test_loss_temp_penalty_nonzero`: loss includes `max(0, t_i - t_{i+1})` summed.
3. Test `test_loss_val_penalty_nonzero`: loss includes |Σ_in - Σ_out| / Σ_in.
4. Test `test_loss_cls_bce`: standard BCE on cycle labels.
5. Test `test_hard_constraint_recovery`: λ_1, λ_2 → ∞ → output probabilities concentrated on constraint-satisfying cycles only (smoke test on toy graph).
6. Implement `TemporalMessagePassing(nn.Module)` with explicit time-increase filter.
7. Implement `CycleSubgraphEncoder` with attention over cycle nodes.
8. Implement `TC_GNN` wrapper class.
9. Implement combined loss `L = L_cls + λ1·L_temp + λ2·L_val`.
10. Run all tests → pass. Commit.

### Task 3.2: Integration with sketch output

**Step 1:** Add method `TC_GNN.forward(sketch_candidates, G)` that takes a batch of candidate cycles and outputs probabilities.
**Step 2:** Test with toy graph: 5 candidates, 2 are real temporal cycles → top-2 probabilities correlate with ground truth.
**Step 3:** Commit.

**Verification:** All tests pass; model parameter count ≈ 100k–500k (sanity for not-too-large).

---

## Phase 4: Baseline Implementations (~3–5 hr)

### Task 4.1: Static baselines

**Files:**
- Create: `experiments/baselines/static_gcn.py` (GCN + GAT — use PyG built-ins)
- Create: `experiments/baselines/tgn.py` (TGN — use PyG-TGN if available, else port)
- Create: `experiments/baselines/dcrnn.py` (simplified DCRNN)
- Create: `experiments/baselines/glass.py` (GLASS — simplified port or use published code)
- Create: `experiments/baselines/xgb.py` (XGBoost on hand-crafted features)

**For each baseline:**
1. Adapter class that consumes Elliptic2 (nodes + candidate cycles) and outputs per-cycle probability.
2. Smoke test on synthetic graph.
3. Commit per baseline.

### Task 4.2: Baselines table inventory

Write `experiments/baselines/README.md` listing each baseline, original paper, what we modified, why.

---

## Phase 5: Experiment Pipeline (~6–10 hr)

### Task 5.1: Dataset loader (`experiments/data_loader.py`)

**Files:**
- Create: `experiments/data_loader.py`

**Step 1:** Function `load_elliptic2_candidates()`:
  - Load Elliptic2 nodes/edges from `data/elliptic2/processed/`
  - Generate candidate cycles via `TemporalCycleSketch` (with realistic Δt, k range)
  - Label candidates: positive if cycle overlaps with any of 49 illicit subgraphs
  - Split train/val/test by time_step (temporal split, no leakage)
**Step 2:** Smoke test: print n_train, n_val, n_test, positive ratio.

### Task 5.2: Training script (`experiments/train_tc_gnn.py`)

**Files:**
- Create: `experiments/train_tc_gnn.py`

**Step 1:** PyTorch training loop: BCE+constraint loss, Adam, lr scheduler, early stopping on val AUC-PR.
**Step 2:** Save best checkpoint to `results/checkpoints/tc_gnn_best.pt`.
**Step 3:** Log training curves to `results/logs/tc_gnn_{run_id}.json`.

### Task 5.3: Evaluation script (`experiments/eval_all.py`)

**Files:**
- Create: `experiments/eval_all.py`

**Step 1:** Load TC-GNN checkpoint + all baselines.
**Step 2:** For each, compute AUC-ROC, AUC-PR, F1 on test set.
**Step 3:** Compute constraint satisfaction rate (fraction of predicted positive cycles that satisfy time-increase and value-conservation).
**Step 4:** Save results table to `results/main_results.csv`.
**Step 5:** Smoke: run on small subset (10% data) and verify all baselines produce output.

### Task 5.4: Ablation runs (`experiments/ablation.py`)

**Step 1:** Train 3 ablations: (a) no time-increase constraint (λ1=0), (b) no value conservation (λ2=0), (c) no sketch (use random candidates).
**Step 2:** Save to `results/ablation.csv`.

### Task 5.5: Sensitivity analysis (`experiments/sensitivity.py`)

**Step 1:** Sweep sketch width w ∈ {4, 8, 16, 32}, depth d ∈ {2, 4, 8}, Δt ∈ {1d, 3d, 7d, 14d}, ε ∈ {0.01, 0.05, 0.1, 0.2}.
**Step 2:** Save to `results/sensitivity.csv`.

### Task 5.6: Case study visualization (`experiments/case_study.py`)

**Step 1:** Pick top-5 predicted illicit cycles.
**Step 2:** Render ring plots + time/value heatmaps to `figures/case_studies/`.

**Verification:** All result CSVs exist and have plausible numbers (F1 > 0.5 for TC-GNN, baselines 0.2–0.6).

---

## Phase 6: English Manuscript (`paper/main_en.tex`) (~8–12 hr)

### Task 6.1: Front matter + Section 1 (Introduction)

**Files:**
- Create: `paper/main_en.tex` (IEEEtran skeleton)
- Create: `paper/refs.bib`
- Create: `paper/abstract.tex`

**Content:**
- Title, authors (use placeholder *Author Name* since we don't know actual names — flag for user to fill)
- Abstract (~250 words, integrate motivation + contributions + result)
- Keywords
- Section 1 with 1.1 Background, 1.2 Challenges, 1.3 Contributions, 1.4 Roadmap

### Task 6.2: Section 2 (Problem Formalization)

**Files:**
- Modify: `paper/main_en.tex`

**Content:**
- 2.1 Dynamic graph definition
- 2.2 Temporal k-cycle definition (5 constraints C1–C5)
- 2.3 TCD decision problem
- 2.4 NP-completeness theorem + proof sketch (full proof in Appendix A)
- 2.5 Structural-isomorphism argument (trade ↔ crypto)

### Task 6.3: Section 3 (TemporalDFS exact algorithm)

**Content:** algorithm pseudocode, complexity analysis, comparison with static k-cycle.

### Task 6.4: Section 4 (TemporalCycleSketch approximate)

**Content:** sketch construction, algorithm, approximation guarantees, synergy with TemporalDFS.

### Task 6.5: Section 5 (TC-GNN architecture)

**Content:** three-layer architecture diagram, constraint-regularized loss, comparison table with TGN/DCRNN.

### Task 6.6: Section 6 (Experiments)

**Content:**
- 6.1 Dataset (Elliptic2 stats — pull from Task 1.2)
- 6.2 Baselines (table)
- 6.3 Metrics
- 6.4 Main results (pull numbers from `results/main_results.csv`)
- 6.5 Ablation results (from `results/ablation.csv`)
- 6.6 Sensitivity (from `results/sensitivity.csv`)
- 6.7 Case studies (from `figures/case_studies/`)

### Task 6.7: Sections 7–8 (Related Work + Conclusion)

**Content:** related work categories (DGNN, subgraph/cycle detection, AML, trade fraud), conclusion with limitations and future work (海关合作, 多模态, 联邦学习).

### Task 6.8: Appendices A–D

**Content:**
- A. Full NP-completeness construction (3-SAT reduction)
- B. Detailed TemporalCycleSketch complexity derivation
- C. TC-GNN hyperparameter table + training curves
- D. Extra sensitivity analysis (different k, Δt)

### Task 6.9: References

**Files:**
- Modify: `paper/refs.bib`

**Step 1:** Collect 40–50 references from outline-specified categories:
  - DGNN: TGN (Rossi et al. 2020), DCRNN (Li et al. 2018), ST-GCN (Yan et al. 2018), EvolveGCN (Pareja et al. 2020)
  - Cycle/subgraph: Alon-Yuster-Zwick (1995), FAST temporal motifs (Paranjape et al. 2017), GLASS (Yuan et al. 2021)
  - AML: Elliptic1 (Weber et al. 2019), Elliptic2 (rel. 2024), RevTrack (rel. 2023), Chainalysis reports
  - Trade/fraud: FATF typologies, 海关稽查 literature
  - Theory: 3-SAT NP-completeness (Karp 1972), Hamiltonian cycle
**Step 2:** Use WebFetch + WebSearch as needed to confirm bibliographic details.

**Verification:** `cd paper && pdflatex main_en.tex && bibtex main_en && pdflatex main_en.tex && pdflatex main_en.tex` exits 0; PDF has all 8 sections + 4 appendices + references; word count 12k–15k.

---

## Phase 7: Chinese Manuscript (`paper/main_zh.tex`) (~5–8 hr)

### Task 7.1: Independent Chinese translation

**Files:**
- Create: `paper/main_zh.tex` (xeCJK + ctexart class)
- Create: `paper/refs_zh.bib` (Chinese references where available, English refs bilingual)

**Strategy:**
- Translate section by section, NOT relying on automatic tools
- Preserve all math/algorithm notation (English variable names in Latin script)
- Use 国际贸易/动态图神经网络/反洗钱 terminology consistently
- Figures/tables: same source files as English version (TikZ code shared)

**Verification:** `cd paper && xelatex main_zh.tex && bibtex main_zh && xelatex main_zh.tex && xelatex main_zh.tex` exits 0; PDF compiles cleanly; Chinese terminology glossary in `paper/glossary_zh.md`.

---

## Phase 8: Polish & Verification (~2–4 hr)

### Task 8.1: Cross-check consistency

**Step 1:** Diff all numbers between `main_en.tex` and `main_zh.tex` (table values, theorem statements, code references).
**Step 2:** Verify all figures in `figures/` are referenced in both manuscripts.
**Step 3:** Verify all `results/*.csv` data points match paper claims.

### Task 8.2: Final commit + summary

**Step 1:** `git add -A && git commit -m "feat: complete TCD paper v1 (zh+en) + TC-GNN implementation + Elliptic2 experiments"`
**Step 2:** Write `DELIVERABLES.md` listing: PDFs produced, code modules, result CSVs, instructions to reproduce.
**Step 3:** Tag git `v1.0-submission-ready`.

**Verification:** `git tag -l` shows tag; DELIVERABLES.md exists; both PDFs render.

---

## Critical Risks & Mitigations

| Risk | Mitigation |
|---|---|
| Elliptic2 download fails | Fallback ladder (Elliptic1 → synthetic) explicitly documented in §6.1 + §8.2 |
| TNNLS template needs exact IEEEtran | Use `\documentclass[journal]{IEEEtran}`; verify word count after compilation |
| LaTeX toolchain not installed | Fallback: write both .tex files but compile via Overleaf (zip in `paper/overleaf_export/`) |
| Code tests fail | Stop, debug, fix — never ship broken tests |
| Word count overshoots | Move depth from main body to appendix (already designed this way) |
| Chinese chars not in fonts | Use `ctexart` + system fonts; document required `texlive-lang-chinese` |

---

## Total Estimated Effort

- Phase 0: 0.5 hr
- Phase 1: 1–2 hr (with retries)
- Phase 2: 6–10 hr
- Phase 3: 4–6 hr
- Phase 4: 3–5 hr
- Phase 5: 6–10 hr
- Phase 6: 8–12 hr
- Phase 7: 5–8 hr
- Phase 8: 2–4 hr
- **Total: ~35–57 hr of autonomous work**

After your approval, I will execute this end-to-end with no mid-execution checkpoints (per your "一口气出完整初稿" preference), surfacing only blockers that genuinely require your input (e.g., author names, dataset truly unreachable).